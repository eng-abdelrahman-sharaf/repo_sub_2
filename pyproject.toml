[build-system]
requires = ["setuptools>=40"]
build-backend = "setuptools.build_meta"

[project]
name = "repo-sub-2"
version = "0.1.0"
description = "UETrack library"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]
include = ["lib*"]
